@using CvPlatform.Web.Components.Account.Shared
@layout AccountLayout
@* These pages write cookies, which needs the HTTP response: they must stay static SSR. *@
@attribute [ExcludeFromInteractiveRouting]
