namespace CvPlatform.Domain;

// Code finds system attributes by this key: their ids differ per database and their names are editable.
public enum SystemAttributeKey
{
    FirstName,
    LastName,
    Location,
    Photo,
}
